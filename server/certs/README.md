# Local Test Certificates

Run `./scripts/generate-cert.sh` from the repository root to create `server.crt` and `server.key`.

These files are self-signed local test certificates and are intentionally ignored by Git.
